# Bash Installer

This project contains `install.sh`, a simple Bash script that updates the package lists and installs curl on a Linux (Debian/Ubuntu) machine. It was made to practice the Git workflow: branches, merging and pushing to GitHub.

## How to use (on Linux)

    chmod +x install.sh
    ./install.sh
